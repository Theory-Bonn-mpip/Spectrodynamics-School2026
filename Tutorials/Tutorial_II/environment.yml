channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.12
  - pip
  - pip:
    - ase
    - chemiscope>=1.0
    - ipi>=3.2.0  
    - matplotlib
    # MACE with the MDP atomic-decomposition outputs (STREAM branch)
    - git+https://github.com/MPIP-STREAM/mace.git@MDP-derivatives-with-bec_training
    - jupyter
    - jupytext
