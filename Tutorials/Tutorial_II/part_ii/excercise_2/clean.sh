# Run this script to clean up the directory after a simulation run. It will remove temporary files, outputs and logs.
rm -f \#* nvt2.* EXIT ipi.log mace_*.log RESTART Cvv.dat Cvv_spectrum.dat .input_run.xml
